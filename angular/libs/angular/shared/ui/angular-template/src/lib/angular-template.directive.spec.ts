import { AngularTemplateDirective } from './angular-template.directive';

describe('AngularTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AngularTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
